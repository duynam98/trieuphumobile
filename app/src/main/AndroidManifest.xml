<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.duynam.ailatrieuphu">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/noactionbar">
        <activity android:name=".activity.KetquaThachdauActivity"
            android:parentActivityName=".activity.ManHinhChinhActivity"
            android:theme="@style/Custorm" ></activity>
        <activity android:name=".activity.TrangcanhanActivity" />
        <activity android:name=".activity.BangxephangActivity" />
        <activity
            android:name=".activity.ManHinhChinhActivity"
            android:label="@string/home" />
        <activity
            android:name=".activity.KetquaActivity"
            android:label="@string/title_toolbar_ketqua"
            android:parentActivityName=".activity.ManHinhChinhActivity"
            android:theme="@style/Custorm" />
        <activity android:name=".activity.ChoithuActivity" />
        <activity android:name=".activity.LoginActivity" />
        <activity
            android:name=".activity.SplashActivity"
            android:theme="@style/noactionbar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".service.NhacnenService"
            android:enabled="true"
            android:exported="true" />
    </application>

</manifest>